# 项目总结：boss_spider

本项目是一个针对 BOSS 直聘网站的职位信息采集与数据可视化工具集合，包含两个同级模块：数据抓取引擎和前端分析看板。

## 1. 数据抓取 (`spider/`)
- **技术栈**：Python, Selenium, PyMySQL
- **核心逻辑**：基于 Selenium 模拟浏览器行为，优化了 DOM 元素定位规则。脚本启动时会注入 JavaScript 隐藏 webdriver 属性以降低风控概率，并内置了针对登录遮罩弹窗的自动关闭逻辑。
- **定制化与存储**：支持设置目标城市、岗位分类（大类及子类）、多关键字循环搜索以及最大翻页深度。提取的数据涵盖职位基础信息、薪资结构（支持匹配 K/薪 等规则）、公司画像（行业、融资、规模）及任职要求，最后通过 `dbutils.py` 传入本地 MySQL 数据库并处理去重。

## 2. 数据可视化 (`dashboard/`)
- **技术栈**：HTML, Vanilla JavaScript, ECharts
- **核心逻辑**：纯前端实现的静态数据面板。通过读取抓取后的数据结果集开展多维度交叉分析。
- **主要图表**：包含了大盘薪资直方图、不同公司规模下的薪资区间分布（箱线图）、特定行政区及子商圈层级的嵌套饼图。同时，内置了基于特定关键词的岗位置信度分类逻辑（区分诸如 B端、数据、AI/算法等细分方向）来对比需求量与平均薪资，外加高频福利标签的分类提点与职位动态流面板。
